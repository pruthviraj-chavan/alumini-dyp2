from django.db import models

# The core app doesn't need additional models as it will primarily handle views and templates
# All the required models are defined in the accounts, posts, and chat apps
